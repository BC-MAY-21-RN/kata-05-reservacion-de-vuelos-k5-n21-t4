import React, {useState} from 'react';
import {Container, Texto} from '../Assets/styled';

export const MyFlights = ({navigation}) => {
  return (
    <Container>
      <Texto size={'16px'}>Hola mundo</Texto>
     
    </Container>
  );
};